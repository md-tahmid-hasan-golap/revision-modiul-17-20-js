// task number - 1

var drinksPrice = 500;
if(drinksPrice >= 500){
    // 3 % discount
    var discount = drinksPrice * 3 / 100;
    var payout = drinksPrice - discount;
    console.log(payout);
    console.log(discount)
}
else if(drinksPrice < 400){
    // 3 % jorimana
    var jorimana = drinksPrice + 30;
    var payout = drinksPrice + jorimana
}

// task number - 2


